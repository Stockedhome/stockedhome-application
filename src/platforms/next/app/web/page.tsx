import { HomeScreen } from 'interface/features/home-screen'

import {metadata as layoutMetadata } from './layout';

export const metadata = {
    ...layoutMetadata,
    title: {
        absolute: 'Home | Stockedhome Web',
    },
}

export default HomeScreen
